version: '3'
services:
  app:
    build: .
    ports:
      - "3000:4000"
    depends_on:
      - mysql
    environment:
      DB_HOST: mysql
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: riya_gandhi_05
      DB_NAME: student_management
    command: sh -c "sleep 15 && node server.js"

  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: riya_gandhi_05
      MYSQL_DATABASE: student_management